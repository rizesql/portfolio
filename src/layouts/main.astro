---
import BaseLayout from "./base.astro";

import Navbar from "~/components/navbar.astro";
import { main } from "~/components/ui";
import { cx } from "~/lib/cx";

export type { Props } from "./base.astro";

const { class: className, "class:list": classList, ...props } = Astro.props;
---

<BaseLayout
	class={cx(
		"px-4 lg:px-6 relative w-full lg:w-screen bg-background text-foreground @container/root",
		className,
		classList,
	)}
	{...props}
>
	<Navbar />

	<main class={main()}>
		<slot />
	</main>
</BaseLayout>
