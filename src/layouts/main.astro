---
import BaseLayout, { type Props as BaseProps } from "./base.astro";

import Navbar from "~/components/navbar.astro";
import { cx } from "~/lib/cx";

export type Props = BaseProps;

const { class: className, "class:list": classList, ...props } = Astro.props;
---

<BaseLayout class={cx("px-4 lg:px-6 relative w-screen", className, classList)} {...props}>
	<Navbar />

	<main class="grid gap-x-4 gap-y-32 pt-4 md:grid-cols-2 lg:grid-cols-12">
		<slot />
	</main>
</BaseLayout>
